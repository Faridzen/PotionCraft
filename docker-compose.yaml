version: '3.8'

services:
  api:
    build: ./api
    ports:
      - "8081:80"

  frontend:
    build: ./frontend
    volumes:
      - ./frontend:/usr/share/nginx/html
    depends_on:
      - api
    ports:
      - "8080:80"